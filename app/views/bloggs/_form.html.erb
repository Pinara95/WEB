<%= form_with(model: blogg, local: true) do |form| %>
  <% if blogg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blogg.errors.count, "error") %> prohibited this blogg from being saved:</h2>

      <ul>
      <% blogg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :adresse %>
    <%= form.text_field :adresse %>
  </div>

  <div class="field">
    <%= form.label :plz %>
    <%= form.text_field :plz %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
